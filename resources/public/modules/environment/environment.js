"use strict";

var environmentModule = angular.module("environmentModule", []);

environmentModule.controller("environmentController", ["$rootScope", "$scope",
    function($rootScope, $scope){
    }
]);


